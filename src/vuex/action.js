//提交的事mutation,不是直接改变状态,可以包含任何异步操作
//action通过$store.dispatch来触发
//context.commit提交一个mutation;也可通过context.state和context.getters获取state和getters
export default
{
  nav({context}){//接受一个与store实例具有相同方法和属性的context

  }
}
